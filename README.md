# pjt-shopai

> AI 리뷰 요약 + 재고 동시성 처리를 핵심으로 한 커머스 플랫폼
> 5년차 백엔드 개발자 이직 포트폴리오 프로젝트

---

## 📌 프로젝트 소개

단순 기능 구현을 넘어 **설계 / 성능 / 인프라** 역량을 함께 어필하기 위한 커머스 플랫폼입니다.

- **AI 리뷰 요약** — Gemini API + 2단계 캐싱(Redis + DB)으로 상품 리뷰를 자동 요약
- **재고 동시성 처리** — MySQL 비관적 락(SELECT FOR UPDATE)으로 동시 주문 시 overselling 방지
- **주문 스냅샷** — 주문 시점의 상품명/가격을 별도 저장해 데이터 일관성 보장
- **GW - AP - DB 서버 분리** — Nginx Gateway에서 JWT 인증/라우팅 처리

---

## 🛠️ 기술 스택

| 영역 | 기술 |
|------|------|
| 백엔드 | NestJS 11 (TypeScript) |
| 프론트엔드 | React 19 (Vite + TypeScript) |
| AI | Google Gemini API |
| 데이터베이스 | MySQL |
| 캐싱 | Redis |
| 인프라 | Docker, GitHub Actions |
| 서버 구조 | Nginx (GW) — NestJS (AP) — MySQL (DB) |
| 결제 | 포트원 (테스트 모드) |

---

## 🏗️ 서버 아키텍처

```
클라이언트
    ↓
GW 서버 (Nginx) — JWT 검증 (auth_request)
    ↓
AP 서버 (NestJS:3030) + Redis
    ↓
DB 서버 (MySQL)
```

---

## 📁 프로젝트 구조

```
pjt-shopai/
├── docs/
│   └── database/
│       ├── erd/              # ERD 이미지
│       ├── schema/           # 테이블 정의서 (11개 md 파일)
│       └── scripts/          # DDL 및 시드 데이터 SQL
├── backend/                  # NestJS 백엔드
│   └── src/
│       ├── common/           # Guards, Filters, Interceptors, Decorators
│       ├── config/           # 환경 설정
│       ├── database/
│       │   └── seeds/        # 초기 데이터 (카테고리, 상품, 리뷰, 유저)
│       ├── modules/
│       │   ├── auth/         # 인증 (JWT)
│       │   ├── users/        # 회원
│       │   ├── addresses/    # 배송지
│       │   ├── categories/   # 카테고리
│       │   ├── products/     # 상품
│       │   ├── reviews/      # 리뷰
│       │   ├── ai/           # Gemini API + 캐싱 로직
│       │   ├── carts/        # 장바구니
│       │   ├── orders/       # 주문 (비관적 락)
│       │   └── payments/     # 결제 (포트원)
│       └── infrastructure/   # Redis, 포트원 외부 연동
├── frontend/                 # React 프론트엔드
│   └── src/
│       ├── api/              # API 호출 함수
│       ├── components/       # 공통 컴포넌트
│       ├── pages/            # 페이지
│       ├── hooks/            # 커스텀 훅
│       ├── store/            # 상태 관리 (Zustand)
│       └── types/            # TypeScript 타입 정의
├── docker/
│   ├── backend.Dockerfile
│   └── frontend.Dockerfile
├── docker-compose.yml        # 운영 AP 서버용 (Redis + NestJS)
├── .env.example
└── README.md
```

---

## ⚙️ 로컬 실행 방법

### 사전 준비
- Node.js 20+
- Redis (`brew install redis && brew services start redis`)
- MySQL (별도 DB 서버 연결)

### 1. 레포지토리 클론

```bash
git clone https://github.com/Neur0n-dev/pjt-shopai.git
cd pjt-shopai
```

### 2. 환경변수 설정

```bash
cp .env.example .env
# .env 파일에 실제 값 입력
```

### 3. 백엔드 실행

```bash
cd backend
npm install
npm run seed        # 초기 데이터 삽입
npm run start:dev
```

### 4. 프론트엔드 실행

```bash
cd frontend
npm install
npm run dev
```

### 접속 URL

| 서비스 | URL |
|----------------------|-------------------------------|
| 프론트엔드 | http://localhost:5173 |
| 백엔드 API | http://localhost:3030 |
| Swagger (관리자 기능) | http://localhost:3030/api-docs |

---

## 🔑 테스트 계정

```
# 일반 유저
email: user@shopai.com
password: test1234

# 관리자
email: admin@shopai.com
password: admin1234
```

---

## 🌟 핵심 구현 설명

### 1. 재고 동시성 처리 (비관적 락)

100명이 동시에 재고 1개짜리 상품을 주문할 때 overselling이 발생하지 않도록 MySQL 비관적 락을 적용했습니다.

```sql
-- 주문 생성 시 해당 상품 행을 잠금
SELECT * FROM products WHERE id = ? FOR UPDATE;

-- 재고 차감 후 커밋
UPDATE products SET stock = stock - 1 WHERE id = ?;
COMMIT;
```

### 2. AI 리뷰 요약 (2단계 캐싱)

매 요청마다 Gemini API를 호출하지 않고 Redis + DB 2단계 캐싱으로 비용과 응답속도를 최적화했습니다.

```
요청
 ↓
Redis 확인 → 있으면 즉시 반환 (약 10ms)
 ↓ 없으면
DB(review_summaries) 확인 → 있으면 Redis 저장 후 반환
 ↓ 없으면
Gemini API 호출 → Redis + DB 저장 후 반환
```

### 3. 주문 스냅샷

주문 시점의 상품명과 가격을 `order_items`에 복사 저장해, 이후 상품 정보가 변경되어도 주문 내역의 데이터 일관성을 보장합니다.

---

## 🚧 개발 현황

| 구분 | 상태 | 내용 |
|------|------|------|
| DB 설계 | ✅ 완료 | ERD, 테이블 정의서(11개), DDL 스크립트 |
| Week 1 | ✅ 완료 | TypeORM 연결, 환경변수, ValidationPipe, Interceptor, ExceptionFilter, Swagger |
| Week 2 | 🔄 진행 중 | 회원가입·로그인·토큰 재발급 API 완료 / 로그아웃·JwtAuthGuard·RolesGuard·@CurrentUser() 구현 예정 |
| Week 3~12 | ⏳ 예정 | 상품, 주문, 결제, AI 요약, 프론트엔드, 인프라 |

---

## 📊 성능 테스트 (k6)

> 추후 작성 예정

---

## 📅 개발 기간

2026년 2월 ~ 2026년 4월 (약 3개월)

---

## 👤 개발자

- GitHub: [@Neur0n](https://github.com/Neur0n-dev)